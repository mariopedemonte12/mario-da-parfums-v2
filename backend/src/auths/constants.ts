// Name of the httpOnly session cookie set by `login`/`register` and read by
// `JwtAuthGuard`. See specs/auth-pages.md ("Contract assumption") for the
// frontend side of this contract.
export const SESSION_COOKIE_NAME = 'session';
