# TODO

- Run mutation testing (`pnpm test:mutation`, powered by Stryker + Vitest, config in `stryker.config.json`) and review the mutation score / surviving mutants. Not run yet as part of the tooling setup — needs to be executed against the current test suite at some point.
