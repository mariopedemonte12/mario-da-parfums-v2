[pytest]
markers =
    integration: requires a real Postgres (see backend/docker-compose.yml)
    real_model: instantiates the real sentence-transformers model (slow/heavier -- run explicitly with `-m real_model`)
testpaths = tests
addopts = -m "not real_model"
