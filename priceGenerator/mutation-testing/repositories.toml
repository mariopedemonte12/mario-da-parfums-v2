[cosmic-ray]
module-path = [
    "priceGenerator/fragrance_reader.py",
    "priceGenerator/vendor_repository.py",
    "priceGenerator/listing_repository.py",
]
timeout = 20.0
excluded-modules = []
test-command = "priceGenerator/.venv/bin/python -m pytest priceGenerator/tests -m integration -q"

[cosmic-ray.distributor]
name = "local"
